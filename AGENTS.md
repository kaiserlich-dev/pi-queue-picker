# Pi Queue Picker

## Testing

Always use the `tmux-feature-test` skill to test changes before committing. This is a TUI extension — compile checks alone are not sufficient. Every behavioral change must be verified in a tmux session with captured pane output as evidence.
